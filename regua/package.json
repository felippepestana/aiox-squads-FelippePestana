{
  "name": "@aiox/regua-core",
  "version": "0.1.0",
  "private": true,
  "description": "RÉGUA / Toga Recupera — core domain: portfolio ingestion, prescription engine, aging (Sprint 1 of docs/regua/03-backlog-mvp.md)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/**/*.test.js\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  }
}
