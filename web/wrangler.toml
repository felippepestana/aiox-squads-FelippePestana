name = "aiox-squads-web"
main = "dist/worker/index.js"
compatibility_date = "2026-03-20"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm ci && npm run build && node scripts/bundle-squads.mjs"

[assets]
directory = "./dist/client"

[vars]
NODE_ENV = "production"
