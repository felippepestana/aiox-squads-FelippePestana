/**
 * Generates a Markdown report from the analysis result.
 * This serves as the base content for PDF export.
 */
export async function generateMarkdownReport(
  analysisResult: unknown,
  mode: string
): Promise<string> {
  // TODO: build full markdown from structured analysis result
  return "# Relatorio\n\nEm construcao...";
}
